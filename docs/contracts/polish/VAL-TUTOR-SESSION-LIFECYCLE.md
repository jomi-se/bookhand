# VAL-TUTOR-SESSION-LIFECYCLE: Temporary guidance remains learner-controlled

Surface: application session/navigation controller, command API, genuine WebMCP, reader and study UI, and browser.
Needs: shared observable reader navigation and board state.
Behavior: One origin-aware controller owns every deliberate reader movement: page controls and keys, Foliate swipe or in-book link, Contents, Search result, Study source, ordinary command navigation, and tutor navigation. A relocation is classified as an issued-move completion, captured learner navigation intent, or incidental reflow. Resize, panel composition, pagination, and style reflow update reader geometry without yielding guidance or releasing its persistence anchor. Learner swipe and in-book-link intent is captured before Foliate moves. A learner move retires every unsettled agent token. If a retired move physically changes Foliate's viewport before emitting its late relocation, the controller reasserts the newest authoritative learner or tutor target before settling either action; stale state never becomes authoritative merely because the renderer moved.

The controller owns a runtime-monotonic generation that never resets when a session clears. Every asynchronous continuation captures generation, book identity, and adapter identity and validates all three before each reader, session, panel, focus, overlay, or storage effect. Back, Stop, takeover, close, detach, and new-session creation invalidate older generations. `absent -> guiding` captures the learner origin; `guiding -> guiding` preserves it; `yielded -> guiding` starts a fresh session at the learner-owned takeover state. One runtime-owned, never-persisted session records that generation, current book, active target, status `guiding|yielded`, and one session-origin location, panel, board view, and focus target.

Guidance pins the location value used by reading-state persistence to its origin while the mounted reader shows the guided location; style changes continue to persist in the same record. Learner navigation changes the session to `yielded`, releases the persistence anchor to the learner's new location, and leaves one quiet Back affordance. Back restores the session origin and ends guidance, returning exactly `restored`, successful `no_back_target`, or visible `unresolvable`; it is not a per-hop stack. An unresolvable Back leaves the reader where it is and accepts that current location as learner-owned. Stop returns exactly `{ outcome: "cleared", wasActive, guidance }`, never navigates, is idempotent, accepts the current location as learner-owned, and clears guidance. `control_guidance` accepts exactly `{ action: "back" | "stop" }`. `get_reading_context` adds `guidance: { state: "absent"|"guiding"|"yielded", canBack: boolean, revision: number }` and never exposes location history. `guidance.revision` is the public value of the controller's runtime-monotonic generation, including while guidance is absent, and never resets during the mounted runtime. A compact attributed indicator is mounted outside panel replacement and chrome recession: `guiding` always offers Back and Stop; `yielded` offers Back and Stop without claiming control. Book close, adapter detach, and unmount flush or retain the origin location before clearing its anchor; reload restores that learner-owned position with no session. Guidance cannot change annotations, lessons, styles, or preferences.

Evidence: Serialized command/tool schemas and genuine calls; exact focus, Back, Stop, and guidance-state result unions; deterministic races at every await boundary for Stop, Back, takeover, new focus, close, and detach; generation/book/adapter invalidation and public revision monotonicity while active and absent; intent classification for page controls/keys, swipe/link, Contents, Search, Study, ordinary commands, tutor commands, resize, panel replacement, pagination, and style reflow; reverse-order and late-relocation races proving the DOM-visible passage, adapter location, reading context, and persisted location all end at the newest authoritative target; exact stored reading-state values and write order across active guidance, style writes, takeover, Stop, restored/unresolvable Back, detach, unmount, and reload; indicator survival after five seconds of chrome recession and compact panel replacement; preference/content snapshots; mobile, keyboard, and reduced-motion behavior.
