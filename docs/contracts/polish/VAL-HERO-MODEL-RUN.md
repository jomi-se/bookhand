# VAL-HERO-MODEL-RUN: A real model composes the deployed hero lesson

Surface: deployed secure origin in a compatible agent browser.
Needs: `VAL-AGENT-DESIGN-CONTEXT`, `VAL-SEARCH-BOOK`, `VAL-STUDY-EXPERIENCE-LIFECYCLE`, `VAL-DEPLOYED-RUNTIME-TRUTH`, and the deployed commit hash.
Behavior: A named model/version receives an intent-only request to help with the open Chapter X passage; the user prompt contains no design-system prose, lesson prose, citations, equation answer, plot data/configuration, coordinates, or tool payload. The named model itself discovers and originates tool calls through real `document.modelContext`, calls `get_design_context` for Study before composing, reads grounded results, and supplies the lesson prose, citations, equation, declarative plot data/AST, and question; a second intent-only follow-up updates the same experience; reload preserves it; a deliberately invalid follow-up produces visible bounded failure. Replayed model output, deterministic relay/orchestration, direct handler/command calls, or application/test/prompt-fixture hard-coding cannot satisfy this contract.
Evidence: Fresh independently witnessed transcript and runtime trace naming model, version, compatible agent browser, WebMCP runtime, deployed commit/assets, and secure origin; exact user prompts; model-originated ordered design-context, retrieval, and mutation calls with bounded results; model-supplied payload; visible creation/follow-up/error/reload recording; repository inspection covering application, tests, prompt fixtures, and orchestration scripts and showing the final lesson copy/configuration is absent.
