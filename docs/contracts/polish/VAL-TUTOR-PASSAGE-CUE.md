# VAL-TUTOR-PASSAGE-CUE: Temporary emphasis points without becoming an annotation

Surface: reader adapter, isolated tutor-overlay renderer, genuine WebMCP, and browser.
Needs: `VAL-TUTOR-PASSAGE-FOCUS`, `VAL-TUTOR-OVERLAY-ISOLATION`, and `VAL-RANGE-OWNERSHIP`.
Behavior: `focus_passage` may additionally accept `cue?: { kind: "underline"|"highlight"|"outline" }` for its already verified active range. The cue uses the isolated tutor-overlay layer, briefly pulses then settles, or uses stable emphasis under reduced motion. A sentence or compact paragraph is presented as one composed cue rather than one mark per inline fragment; a broad range degrades to at most one quiet wash and accent rule per visible column, never dozens of word or equation boxes. Its accent follows the active reader theme. It coexists with every durable highlight at the same CFI; durable annotation rerenders cannot replace it, and clearing or superseding guidance restores all durable marks unchanged. Leaving the active guiding target through learner takeover/yield, new focus, Back, Stop, close, detach, book change, or reload removes cue and invalidates scheduled cue work. It is never persisted, creates no annotation or study item, and cannot be installed without a successfully active focus target.
Evidence: Serialized cue schema; genuine focus calls for all three kinds; compact and broad exact target geometry with a bounded rendered-element count; theme-derived accent; optional message; same-CFI durable-highlight coexistence before and after annotation rerender; generation-guarded delayed-render races; takeover/yield, new focus, Back, Stop, close, detach, book change, and reload absence; exact storage snapshots; keyboard, mobile placement, contrast, zoom, and reduced-motion behavior.
